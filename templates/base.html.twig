<!DOCTYPE html>
<html>
    <head>
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text></svg>">

        <link rel="stylesheet" href="{{ asset('css/menu.css') }}">





        {# Run `composer require symfony/webpack-encore-bundle` to start using Symfony UX #}
        {% block stylesheets %}
            {{ encore_entry_link_tags('app') }}
        {% endblock %}

        {% block javascripts %}
            {{ encore_entry_script_tags('app') }}
        {% endblock %}
    </head>
    <body>

    {%  if app.user %}
        <nav>
            <a href="{{ path("accueil")}}">Accueil</a>
            <a href="{{ path("athlete_selectCategorieAction", {"clubId":14})}}">Classement</a>
            <a href="{{ path("admin_liste_athlete")}}">Athlètes</a>
            <a href="{{ path("admin_liste_evenement")}}">Evenements</a>
            <a href="{{ path("deconnexion")}}">Deconnexion</a>
            <div class="animation start-home"></div>
        </nav>

    {% else %}
        <nav>
            <a href="{{ path("accueil")}}">Accueil</a>
            <a href="{{ path("athlete_listeClub")}}">Club</a>
            <a href="{{ path("athlete_rechercheAthleteAction", {"clubId":14})}}">Rechercher</a>
            <a href="{{ path("athlete_selectCategorieAction", {"clubId":14})}}">Classement</a>
            <a href="{{ path("connexion")}}">Connexion</a>
            <div class="animation start-home"></div>
        </nav>

    {%  endif %}


        {% block body %}
        {% endblock %}
    </body>
</html>




