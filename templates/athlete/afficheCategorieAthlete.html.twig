{% extends 'base.html.twig' %}

{% block title %}Hello AthleteController!{% endblock %}

{% block body %}
    <style>
        .example-wrapper {
            margin: 1em auto;
            max-width: 800px;
            width: 95%;
            font: 18px/1.5 sans-serif;
        }

        .example-wrapper code {
            background: #F5F5F5;
            padding: 2px 6px;
        }
    </style>

    <table>
        <thead>
        <tr>
            <th colspan="2">Classement {{ nomCat.getLongName() }}</th>
        </tr>
        </thead>
        <tbody>
        {% for athlete in athletesCat %}
        <tr>
                <td> {{ athlete.getFirstName() }} </td>
                <td> {{ athlete.getLastName() }} </td>
                <td> {{ athlete.getNbPoints() }} </td>
        </tr>
        {% endfor %}
        </tbody>
    </table>

{% endblock %}